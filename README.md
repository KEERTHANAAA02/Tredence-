# Self-Pruning Neural Network

A feed-forward neural network that learns to prune itself during training
using learnable gate parameters and L1 sparsity regularization on CIFAR-10.

## Run
pip install torch torchvision matplotlib numpy
python self_pruning_nn.py

## Results
See report.md for analysis and gate distribution plot.
